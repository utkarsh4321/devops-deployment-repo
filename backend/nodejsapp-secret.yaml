apiVersion: v1
kind: Secret
metadata:
  name: nodejsapp-secret
  namespace: backend-namespace
type: Opaque
stringData:
  # The following values must be base64-encoded.
  # Use a command like `echo -n "your_password" | base64`
  DB_USER: root
  DB_PASSWORD: mysecretpasswords
